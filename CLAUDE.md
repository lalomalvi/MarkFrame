@AGENTS.md

## Sólo para esta máquina

Las reglas del sistema de Lalo —el escudo `guard.ps1`, lo que nunca se toca, y
cómo trabajar con él— viven en `~\.claude\CLAUDE.md` y `~\CLAUDE.md`. No se
duplican aquí.

`cargo` no está en el `PATH`: llámalo por ruta completa
(`$env:USERPROFILE\.cargo\bin\cargo.exe`) o añádelo al `PATH` de la orden.
